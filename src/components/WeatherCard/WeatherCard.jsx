/* eslint-disable react/prop-types */
/* eslint-disable prettier/prettier */
/* eslint-disable indent */

export const WeatherCard = (/*{ weatherData }*/) => {

    // const { clouds, coord, main, name, sys, timezone, visibility, wind, weather } = weatherData;
    return <div></div>;
}